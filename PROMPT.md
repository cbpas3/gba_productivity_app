I want to build an event-driven productivity web application integrated with a Game Boy Advance WebAssembly emulator that programmatically modifies game memory based on task completion using team agents mode in Claude Code.
So I need you to make a plan for assembling a team of agents.
Goal: Build a Gamified GBA Productivity Emulator Web App.
Technical Goal: Build the app using a team of agents.
Tasks: Look at the agents, assemble a team, and create a plan to help them execute the goal. (Recommendation: assign one teammate to the React frontend UI, one to the Web Worker/mGBA WebAssembly integration, and one specifically to the complex Pokémon memory cryptography).
Technical Requirements: This must be a strictly client-side application. Use React for the frontend, implementing a decoupled Event-Driven Architecture (e.g., Zustand or a custom event bus) to prevent the emulator from blocking the UI thread. Use the mGBA WebAssembly port executed entirely within a Web Worker. For memory injection, utilize SharedArrayBuffer and the Atomics API for synchronous, zero-copy communication between the main thread and the worker. You must include the specific logic for decrypting, modifying, and recalculating the 16-bit checksum for the 100-byte Generation III Pokémon data structures to prevent game corruption. All ROM and .sav file management must be handled locally using the browser's File System Access API (no backend servers for ROM hosting). Finally, ensure the development environment is configured to emit strict Cross-Origin-Opener-Policy and Cross-Origin-Embedder-Policy headers so the browser allows shared memory.
